import java.util.ArrayList;


public class listiterator {
                    
    public static void main(String[] args) {
        ArrayList<Integer> list = new ArrayList<>(); //by default it will be store 10 elements
           //<integer> is a type of Integer object 
        list.add(1); //e is reffered as a object , as because it is implicit conversion ..
        list.add(2);
        list.add(3);  //implicit conversion , they are treadted as objects insted of data
        list.add(4);  //by default it will increase by 50% ..
        
     
      System.out.println(list); 
      
      //iterator to traverse the list ....
      
    
    }
}
